{% for post in posts %}
    <article>
        <header>
            <h2>{{ post.title }}</h2>
        </header>
        <p>{{ post.date|date("M j, Y @ g:i a") }} | {{ post.description }}</p>
        <div class="more">{{ post.more }}</div>
    </article>
{% endfor %}